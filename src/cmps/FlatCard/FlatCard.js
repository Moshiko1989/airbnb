import React, { Component } from 'react'

const FlatCard= (props) =>{

        return(
            <div className="flat-card">
                {props.flat}
            </div>
        )
}

export default FlatCard

